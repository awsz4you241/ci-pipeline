---
platform: linux
image_resource:
  type: docker-image
  source: {repository: docker, tag: dind}
inputs:
  - name: ci-docker
  - name: ci-pipeline
run:
  path: sh
  args:
    - -exc
    - |
      docker daemon &
      cd ci-docker/api-server
      docker build . -t api-server:latest
      exit 
